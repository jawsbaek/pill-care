data/
tests/
docs/
research/
person_sample/
scripts/
# Allow the KURE-v1 prefetch script through so the builder stage can
# bake the embedding model into the image (see Dockerfile stage 1).
!scripts/download_kure_model.py
# HF cache is rebuilt fresh inside the builder stage; exclude any local
# copy from the build context so we don't ship ~400MB twice.
hf-cache/
.claude/
.venv/
.git/
.github/
__pycache__/
*.pyc
*.md
!README.md
.env
.env.example
.DS_Store
**/*.csv
**/*.db
**/*.xls
**/*.xlsx
