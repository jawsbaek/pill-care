# Large data files (download/crawl to reproduce)
data/drug_permit_detail.json
data/한국의약품안전관리원_병용금기약물_20240625.csv

# Personal sample data
person_sample/

# Python
__pycache__/
*.pyc
.venv/

# Environment
.env

# OS
.DS_Store

# HuggingFace cache (KURE-v1 embedder) — recreate via scripts/download_kure_model.py
hf-cache/

# Stale NLI model dir left over from pre-refactor (no longer referenced by code)
models/

# Raw MFDS/HIRA data (downloaded from GCS at runtime, do not commit)
data/*

# Gold set is evaluation asset — tracked in git even though raw MFDS data isn't
!data/gold_set/
!data/gold_set/**
